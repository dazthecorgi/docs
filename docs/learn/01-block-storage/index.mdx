import DocCardList from '@theme/DocCardList';
import BlockStorageDiagram from '@site/src/docs/discover/block-storage/BlockStorageDiagram';

# Foundations: Block Storage

Block storage in Quilibrium is built as a Verifiable Delay Function (VDF) driven proof of storage, with query processing proof of validity.

<BlockStorageDiagram />

<DocCardList />
